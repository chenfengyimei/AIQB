# AIQB 安全策略

感谢你帮助 AIQB 及其部署者保持安全。请不要在公开 Issue、讨论区、Pull Request、社交平台或演示站中披露尚未修复的漏洞细节。

## 支持版本

| 版本 | 安全更新 |
|---|---|
| 当前 `2.x` 最新版本 | 支持 |
| 较旧的 `2.x` 版本 | 仅在问题仍可于最新版复现时受理 |
| `1.x` 及更早版本 | 不支持 |
| 未发布分支、第三方 Fork | 尽力协助，但不承诺补丁 |

开始测试前，请先升级到 GitHub 或 Gitee `master` 分支的最新版本，并确认问题不是部署配置、CDN 缓存或已公开修复的问题。

## 私密报告漏洞

请发送邮件至 **2079937150@qq.com**，主题使用：

```text
[AIQB Security] 简短问题名称
```

报告建议包含：

- 受影响版本、提交哈希和部署方式；
- 受影响的路径、接口或组件；
- 最小化复现步骤或非破坏性验证材料；
- 实际影响、攻击前提和是否需要登录；
- 已知缓解措施或修复建议；
- 是否已经向第三方披露。

请勿发送真实用户数据、后台密码、Cookie、API Token、私钥或完整生产数据库。必要时使用脱敏样本，并在邮件中说明希望采用的加密传输方式。

## 响应与协调披露

- 通常在 **72 小时内**确认收到报告；
- 通常在 **7 天内**完成初步复现和严重性判断；
- 通常在 **14 天内**给出修复、缓解或不受理说明；
- 修复发布时间会根据影响范围、补丁复杂度和部署者升级窗口协调确定。

上述时间是维护目标，不是服务等级承诺。复杂问题可能需要更长时间，但维护者会尽量保持沟通。请在修复发布、受影响部署者获得合理升级时间或双方约定日期之前保密。

## 系统与审查范围

本策略覆盖仓库中的：

- `server/`：公开 API、管理 API、登录会话、采集、同步、在线更新和数据访问；
- `frontend/`：公开前台、管理后台及浏览器端安全边界；
- `scripts/`、`install.sh`、`deploy.sh`、`ecosystem.config.js`：安装、备份、升级和运行流程；
- 默认 Nginx/PM2 部署路径及项目生成的 SQLite、JSON、JSONL 数据；
- 项目维护的 GitHub/Gitee 发布包和默认配置。

第三方 CDN、DNS、邮件服务、上游信息源、操作系统和反向代理本身不属于 AIQB 代码范围；但如果 AIQB 对这些边界的错误使用导致可复现的安全影响，仍属于可报告问题。

## 威胁模型与信任边界

AIQB 通常直接或经 CDN/Nginx 暴露到互联网。公开访客、搜索引擎、采集接口响应、URL 参数、HTTP Header、自定义 RSS/JSON/Atom 内容和外部链接均应视为不可信输入。

管理员登录后可以编辑内容、接口、SEO、邮件、站点设置和执行在线更新，因此管理员账号、会话 Cookie、环境变量、SMTP 凭据、更新令牌及 `server/data/` 是高价值资产。源站主机、受控反向代理和拥有服务器权限的运维人员属于受信任边界。

## 必须保持的安全属性

以下属性失效通常构成可报告问题：

- 未登录用户不能读取管理数据或执行管理操作；
- 所有管理写操作必须经过有效会话与同源校验；
- 管理员密码（包括首次安装口令）不得输出到日志或明文落盘，会话存储不得包含可直接使用的原始 Token；
- 自定义采集地址不得访问本机、内网、链路本地、保留 IP 或通过 DNS/重定向绕过限制；
- 外部响应、请求体、分页、并发、超时和解析规模必须有界，不能轻易耗尽服务资源；
- HTML、URL、Header 与日志中的不可信内容不得形成存储型或反射型脚本执行；
- 路径参数、静态文件和备份/更新流程不得逃逸预期目录；
- 在线更新只能使用允许的仓库和不可变提交，必须验证受信 Ed25519 签名、精确文件集合与逐文件 SHA-256，并且不能覆盖 `server/data/`；
- 公开接口不得泄露密码、密钥、私有采集链路、完整访客 IP 或后台专用数据；
- 多实例环境中的会话、限流、采集锁和统计写入必须保持一致，不能因切换实例绕过控制；
- 安全失败应尽量关闭相关能力并保留旧的有效数据，不能把失败结果发布成可信快照。

## 严重性参考

以下问题通常会被优先处理：

- 无需登录的远程代码执行、任意文件写入或更新链路劫持；
- 管理员认证绕过、会话伪造、跨站管理操作或凭据泄露；
- 可访问云元数据、内网服务或本机服务的 SSRF；
- 可窃取后台会话的持久型 XSS；
- 可跨目录读取 `server/data/`、环境变量、备份或系统文件；
- 低成本、可远程稳定触发且会持续影响公开服务的数据破坏或拒绝服务。

严重性取决于真实可达性、所需权限、影响范围、数据敏感度、是否使用默认配置以及是否存在可靠缓解措施，而不只取决于漏洞类别名称。

## 通常不作为安全漏洞受理

- 不包含敏感数据的版本号、服务状态或公开健康百分比；
- 仅影响已停止支持版本且最新版无法复现的问题；
- 需要服务器 root、数据库文件写权限或已控制管理员账号才能完成的同权限行为；
- 只报告缺少某个通用 Header、扫描器评级或最佳实践，但没有可复现安全影响；
- 社会工程、垃圾信息、流量洪泛、物理攻击及第三方服务自身漏洞；
- 在未获许可的生产环境执行高并发压测、破坏性测试或访问真实用户数据；
- 公开内容来源的准确性、版权争议、SEO 排名或采集接口暂时不可用。

这些说明不是对未知漏洞的预先豁免。如果上述情形能跨越项目安全边界并产生新的实际影响，仍可私密报告。

## 安全测试规则

- 仅测试你拥有或得到明确授权的部署；
- 优先在本地环境和测试数据上验证；
- 不持久访问、不修改、不删除他人数据；
- 不进行拒绝服务、垃圾邮件、批量账号尝试或供应链投毒；
- 取得足够证据后立即停止，不继续扩大影响；
- 不以漏洞、数据或披露作为付款或其他利益的要挟。

善意遵守以上规则并进行负责任披露的研究者，维护者会尽力配合复现、修复与致谢。
